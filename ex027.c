#include <stdio.h>

int main()
{
    printf("\nHello, world.\n");

    float a, b, c;

    printf("\nSize of side A: ");
    scanf("%f", &a);
    printf("Size of side B: ");
    scanf("%f", &b);
    printf("Size of side C: ");
    scanf("%f", &c);

    while ( x == 1 )
        if ( a < b + c && a > ( b - c) )
            printf("Ok condition.\n");
        if ( b < a + c && b > ( a - c ) )
            printf("Ok condition.\n");
        if ( c < a + b && c < ( a - b ) )
            printf("Ok condition.\n");


    return 0;
}